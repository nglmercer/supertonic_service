# =============================================================================
# Supertonic TTS Service - Environment Variables Example
# =============================================================================
# Copy this file to `.env` and adjust the values as needed.
# Do not commit `.env` to version control (it's already in .gitignore)
# =============================================================================

# =============================================================================
# Server Configuration
# =============================================================================

# HTTP Server Port (default: 3000)
# PORT=3000

# HTTP Server Host (default: 0.0.0.0)
# HOST=0.0.0.0

# =============================================================================
# TTS Service Configuration
# =============================================================================

# Output directory for generated audio files (default: ./output)
# TTS_OUTPUT_DIR=./output

# Default voice for synthesis (default: F1)
# Available voices: F1, F2, F3, F4, F5, M1, M2, M3, M4, M5
# TTS_DEFAULT_VOICE=F1

# =============================================================================
# Libp2p Configuration (P2P Networking)
# =============================================================================

# Libp2p TCP port for P2P connections (default: 9000)
# LIBP2P_PORT=9000

# Enable Libp2p mode (auto-discovery via mDNS)
# Set to 'true' to enable P2P mode with automatic peer discovery
# LIBP2P_MODE=true

# Libp2p server multiaddr for direct connection
# Format: /ip4/127.0.0.1/tcp/9000/p2p/<peer-id>
# Example:
# LIBP2P_SERVER=/ip4/127.0.0.1/tcp/9000/p2p/QmYourPeerIdHere
# LIBP2P_SERVER=

# =============================================================================
# Client Configuration (for examples/client.ts)
# =============================================================================

# HTTP Server URL for client connections (default: http://localhost:3000)
# SERVER_URL=http://localhost:3000

# Output directory for client-generated audio files (default: ./output)
# OUTPUT_DIR=./output

# =============================================================================
# Environment
# =============================================================================

# Set to 'test' to skip server startup (for testing)
# NODE_ENV=test
# BUN_ENV=test
